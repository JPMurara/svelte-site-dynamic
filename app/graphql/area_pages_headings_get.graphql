query area_pages_headings_get($per_page: Int = 2000, $page: Int = 1) {
	records(
		per_page: $per_page
		page: $page
		filter: { table: { exact: "area_pages_headings" } }
	) {
		results {
			properties
		}
	}
}
