---
slug: rsc/services_component
layout: ""
searchable: false 
method: get
physical_file_path: views/pages/rsc/services_component.liquid
---
{%- liquid
  graphql query = "services_component_get"
  if query.records.results.first
    assign response = query.records.results.first
  else
    assign response = '{"error": "can not find about resources"}'
    response_status 400
  endif
-%}
{{- response -}}