---
slug: rsc/about_component
layout: ""
searchable: false 
method: get
physical_file_path: views/pages/rsc/about_component.liquid
---
{%- liquid
  graphql query = "about_component_get"
  if query.records.results.first
    assign response = query.records.results.first
  else
    assign response = '{"error": "can not find about component data"}'
    response_status 400
  endif
-%}
{{- response -}}