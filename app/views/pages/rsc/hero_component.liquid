---
slug: rsc/hero_component
layout: ""
searchable: false 
method: get
physical_file_path: views/pages/rsc/hero_component.liquid
---
{%- liquid
  graphql query = "hero_component_get"
  if query.records.results.first
    assign response = query.records.results.first
  else
    assign response = '{"error": "can not find hero component data"}'
    response_status 400
  endif
-%}
{{- response -}}